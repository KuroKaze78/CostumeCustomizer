[ResourceTrue]
type=Buffer
format = R8_UINT
data = 1

[ResourceLoadedDefault]

; Update the following to set the defaults
; Remove the semi-colon on the run = <SOMETHING> to enable that piece as the default.
; Choose a color and mesh combo
;  Color options are listed in KeyCycleColors's run = <Options>
;  Mesh Combo options are listed in KeyCycleMeshes's run = <Options>
[CommandListSetDefault]
if ResourceLoadedDefault === -0.0
  x7 = 0
  run = CommandList\Mods\Costumes\CostumeCustomizer\Marie.ini\Reset
  
  ;Preset Options (x6 represents the x7 == 0 means presets, x6 = 1 use option 1 for the preset)
  
  ; Use a presets
  x7 = 0
  ; Use option 1
  y7 = 1  
  ; Run the Select from the first Content Modpack (Folder "01")
  run = CommandList\Mods\Costumes\CostumeCustomizer\01\Marie.ini\Select
  
  ;DO NOT MODIFY
  ResourceLoadedDefault = ResourceTrue
endif

; Mark suit.
[TextureOverrideNormalSuit]
hash=f96be033
match_first_index=2514
match_priority = -1
run = CommandListSetDefault